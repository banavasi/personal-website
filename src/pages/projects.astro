---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SITE_TITLE } from '../consts';
import { Card, CardContent } from '../components/ui/card';
---

<BaseLayout title={`Projects | ${SITE_TITLE}`} description="Featured projects and work">
	<div class="container mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl relative z-10">
		<!-- Page Header -->
		<section class="py-16 sm:py-20 header-section opacity-0 translate-y-10">
			<div class="max-w-3xl">
				<h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold mb-4 bg-gradient-to-r from-white via-white/90 to-white/70 bg-clip-text text-transparent">
					Projects & Work
				</h1>
				<p class="text-xl text-gray-400 leading-relaxed">
					A showcase of featured projects, contributions, and the impact they've made. Each project represents a unique challenge solved with innovative solutions.
				</p>
			</div>
		</section>

		<!-- Placeholder Note -->
		<section class="pb-20 content-section opacity-0 translate-y-10">
			<Card client:load className="border-white/10 bg-white/5 backdrop-blur-sm">
				<CardContent className="pt-6">
					<div class="text-center py-12">
						<div class="text-6xl mb-6 animate-pulse">🚧</div>
						<h2 class="text-2xl font-bold mb-4 text-white">Projects Coming Soon</h2>
						<p class="text-lg text-gray-400 max-w-2xl mx-auto">
							I'm currently curating a selection of my best work to showcase here. Check back soon to see detailed case studies of enterprise platforms, AI automation frameworks, and open-source contributions.
						</p>
						<p class="text-gray-500 mt-6">
							In the meantime, feel free to explore my <a href="https://github.com/banavasi" target="_blank" rel="noopener noreferrer" class="text-white hover:underline">GitHub profile</a> for open-source contributions.
						</p>
					</div>
				</CardContent>
			</Card>
		</section>

		<!-- Navigation -->
		<section class="py-12 border-t border-white/10 nav-section opacity-0 translate-y-10">
			<div class="flex justify-between items-center flex-wrap gap-4">
				<a href="/about" class="text-gray-400 hover:text-white transition-colors flex items-center gap-2 group">
					<span class="group-hover:-translate-x-1 transition-transform">←</span>
					<span>About</span>
				</a>
				<a href="/skills" class="text-gray-400 hover:text-white transition-colors flex items-center gap-2 group">
					<span>Skills</span>
					<span class="group-hover:translate-x-1 transition-transform">→</span>
				</a>
			</div>
		</section>
	</div>
</BaseLayout>

<script>
	import gsap from 'gsap';
	import { ScrollTrigger } from 'gsap/ScrollTrigger';

	gsap.registerPlugin(ScrollTrigger);

	// Header Animation
	gsap.to('.header-section', {
		opacity: 1,
		y: 0,
		duration: 1,
		ease: 'power3.out',
		delay: 0.2
	});

	// Content Animation
	gsap.to('.content-section', {
		opacity: 1,
		y: 0,
		duration: 1,
		ease: 'power3.out',
		delay: 0.4
	});

	// Nav Section
	gsap.to('.nav-section', {
		scrollTrigger: {
			trigger: '.nav-section',
			start: 'top 95%',
			toggleActions: 'play none none reverse'
		},
		opacity: 1,
		y: 0,
		duration: 1,
		ease: 'power3.out'
	});
</script>
